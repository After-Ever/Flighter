using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Text;

using Flighter;
using Flighter.Input;

namespace FlighterTest
{
    [TestClass]
    public class WidgetNodeBuilderTest
    {
        // AddChildWidget w/ and wo/ inherited children.

        // VV All w/ and wo/ inherited stuff.
        // Stateful
        // Stateless
        // Layout
        // Display

        // If constructed as a built node, returns that.

        // Only builds once.
        // Cannot add children after built.


        [TestMethod]
        public void AddingChildren()
        {
            throw new AssertFailedException();
        }
    }
}
